'use strict';

var modules = {
	description: 'Music module'
};

modules.main = function(client, message, args) {

};

modules.play = function(client, message, args) {
	
};

modules.stop = function(client, message, args) {
	
};

module.exports = modules;